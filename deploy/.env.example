# ============================================================
# Banco de dados (MySQL)
# ============================================================
DB_DRIVER=mysql
DB_HOST=localhost
DB_PORT=3306
DB_NAME=appdb
DB_USER=appuser
DB_PASS=apppass
DB_SCHEMA=

# Credencial root (usada apenas pelo container MySQL)
MYSQL_ROOT_PASSWORD=root

# ============================================================
# Observabilidade (Grafana LGTM)
# ============================================================
OTEL_EXPORTER_OTLP_ENDPOINT=localhost:4317

# ============================================================
# Aplicação
# ============================================================
HTTP_ADDR=:8080
