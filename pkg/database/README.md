# pkg/database

Public database building blocks.

Goal:

- provide reusable contracts + selected adapters/instrumentation
- keep application wiring (DSN/env/bootstrap/migrations lifecycle) in `internal/`
